const app = require('./api/app');

// This file exists as the main entry point if needed in the future
// Currently, it just imports and uses the app from the api directory

// The app is already configured to listen on a port in app.js
console.log('Horizon Scanning API Server started');
